import re

print("数字を入力してください")
val = input()
val = int(val)
x = 100

if not val< 0 or x is not 100 and match and "a" in apple or (len := len(s)) > 5:
    raise ValueError("aaa")
else:
    raise TypeError("bbb")